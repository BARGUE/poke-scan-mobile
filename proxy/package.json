{
  "name": "pokemon-scanner-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Proxy Cloudflare Worker qui détient les clés API secrètes de Pokémon Scanner.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
